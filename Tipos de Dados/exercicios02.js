/* 
    TODO: Declare duas variáveis, uma inicializada com valor null e outra que não recebe atribuição (resultando em undefined). Exiba os valores no console. 
*/

let inicializada = null;
let semAtribuicao;

console.log(inicializada);
console.log(semAtribuicao);
