// É importante aprendermos a compreender as mensagens de erros no js que aparecem no console.

console.log(variavelInexistente);
// Nesse erro ele informa que temos um erro de referencia e que ela não está definida.

// StackTrace é uma pilha de comando chamada internamente pelo node que ocorre de forma silenciosa quando executamos algum programa, vamos identificar ele somente quando ocorrer os erros, pois o caminho irá aparecer no console.