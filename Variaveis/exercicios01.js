/* 
    TODO: Declare três variáveis diferentes (uma para cada tipo: string, número e booleano) e atribua valores a elas. Em seguida, exiba o tipo de cada variável no console.
*/

const str = 'Sou uma string';
const num = 27;
const bool = true;

console.log(typeof (str));
console.log(typeof (num));
console.log(typeof (bool));
