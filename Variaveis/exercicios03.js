/* 
    TODO: Declare duas variáveis, uma contendo um número e outra contendo uma string. Em seguida, combine-as em uma terceira variável usando template strings para montar uma frase e exiba o resultado no console.
*/

const number = 27;
const name = 'Pedro';
const combine = `Olá, meu nome é ${name} e tenho ${number} anos!`

console.log(combine);
